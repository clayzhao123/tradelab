# 2026-04-04

## Session Summary

- Completed a full frontend usability audit and backend contract mapping.
- Refactored frontend from template-mock behavior to real backend-wired interactions.
- Added detailed audit report: `docs/frontend-usability-audit-2026-04-04.md`.

## Key Changes

- `DataContext` was rebuilt to centralize real API refresh flows (dashboard/orders/history/risk), websocket hydration, and symbol/timeframe-aware kline loading.
- `Dashboard` now loads klines by selected symbol/timeframe and can trigger scan runs against backend.
- `ScanTable` and `CandlestickChart` no longer show fake mock data when backend has no data.
- `Orders` page now supports real order placement/cancel flows and displays fills.
- `Strategy` page now creates real persisted strategies and toggles enabled state via backend patch.
- `BotRunner` now starts/stops real runs and can update risk rule fields.
- `Backtest` now analyzes real historical run snapshots (instead of random simulated chart).
- `History` status handling fixed for backend lowercase status values.
- Build config fixed for ESM (`vite.config.ts` now resolves path with `import.meta.url`).
- `tsconfig.app.json` excludes unused template ui library folder from app compile to avoid missing dependency breakage.

## Verification

- Frontend lint: PASS
- Frontend tests: PASS
- Frontend build: PASS
- Workspace build: PASS
- Workspace tests: PASS

## Handoff Notes

- Remaining optimization: frontend chunk size warning (>500kb) can be addressed later with route-level splitting.
- Backtest is now implemented as historical-run analytics (history replay), not a standalone simulation API.

## Implementation Update (Stability-First Plan Applied)

- Introduced unified backend error contract with `code/category/requestId/details` and centralized `AppError` handling.
- Added structured request logs with request-level entity context (`runId`, `orderId`, `strategyId`) via hooks.
- Standardized risk/conflict/not-found errors across routes/services.
- Added websocket event sequence (`seq`) and documented reconnect contract (`snapshot` baseline first).
- Enforced frontend websocket ordering/reconnect guard: require snapshot before incremental events, drop stale/out-of-order events.
- Added typed frontend `ApiError` + unified user-facing error mapping.
- Improved page-level failure handling and retry affordances (Dashboard scan, History detail, ErrorBoundary reload).
- Implemented route-level lazy loading for main pages; build output now split per route.
- Added release docs:
  - `docs/v1-acceptance.md`
  - `docs/runbook.md`
  - `docs/websocket-contract.md`
- Added smoke script: `npm run smoke:v1` (`scripts/v1-smoke.mjs`) for HTTP + websocket baseline checks.

## Verification (Post-Implementation)

- `npm run lint --workspace frontend` PASS
- `npm run test --workspace frontend` PASS
- `npm run test --workspace backend` PASS
- `npm run build` PASS
- `npm test` PASS
- `npm run smoke:v1` PASS

## Context Relay Rule

- Added a handoff policy: when session context approaches 95%, generate a compressed relay markdown under `memory/`.
- Current relay snapshot: `memory/context-handoff-2026-04-04-95pct.md`.

## Continuation (Post-Handoff Completion)

- Re-opened `memory/context-handoff-2026-04-04-95pct.md` and completed the remaining TODO in `Backtest.tsx`.
- Implemented timeframe-driven lookback defaults on Backtest page:
  - `1D -> 365`
  - `1M -> 180`
  - `1Y -> 60`
- Added lookback normalization (`24..2000`) before API submission and at input level.
- Added UI hint text clarifying auto default lookback behavior when switching timeframe.

## Verification (Continuation)

- `npm run lint --workspace frontend` PASS
- `npm run test --workspace frontend` PASS
- `npm run build --workspace frontend` PASS

## Continuation (Frontend Issue Fix Batch)

- Addressed live dashboard "non-real price" complaint with backend-first data-source hardening:
  - switched backend default `MARKET_DATA_PROVIDER` to `real` (with env normalization),
  - changed `.env.example` default accordingly,
  - added real-provider fallback priority: cached last real data > mock fallback.
- Added frontend market source visibility (`Market source: REAL/MOCK`) in dashboard main area metadata.
- Improved strategy delete reliability:
  - frontend now applies optimistic local removal/update for strategies and decouples strategy refresh from runs refresh failures.
  - conflict message for in-use strategy deletion is now explicit and actionable.
- Strategy Lab UX update:
  - moved fusion weight sliders to right panel,
  - new indicators now default to `0%` (no forced equal split on selection),
  - dragging one indicator auto-rebalances others to keep total at `100%`,
  - in-use strategies now show `In Run` badge and delete button disables with reason.
- Added reusable local skill:
  - `skill/frontend-backend-workflow/SKILL.md`
  - `skill/README.md`
  - purpose: whenever frontend changes are requested, enforce frontend+backend contract/workflow validation.

## Verification (Frontend Issue Fix Batch)

- `npm run lint --workspace frontend` PASS
- `npm run test --workspace frontend` PASS
- `npm run build --workspace frontend` PASS
- `npm run test --workspace backend` PASS
- `npm run smoke:v1` PASS

## Continuation (Watchlist Recovery Hardening)

- Real watchlist fallback path refined:
  - if CoinGecko market-cap API fails, backend now attempts Binance quote-volume ranked watchlist first,
  - then cached real watchlist,
  - and only finally static mock fallback.
- Re-ran `npm run test --workspace backend` after this change: PASS.

## Continuation (Backtest Heatmap UX Upgrade)

- Improved backtest symbol readability:
  - symbol formatter now preserves ticker-style abbreviations such as `BTC` instead of truncated `B...`,
  - Top100 symbol tiles were re-laid out so ticker text remains readable in compact cards.
- Expanded backtest API contract for focused-symbol inspection:
  - each symbol result now includes `priceCurve` and paired `tradeMarkers` (buy/sell, timestamp, price, linked exit metadata).
- Upgraded `Focused Symbol Detail`:
  - kept the existing trade-return curve,
  - added a new full-window price-history chart with green buy markers and red sell markers,
  - revised interaction from hover-driven markers to trade-list-driven markers:
    - chart now shows no points by default,
    - profitable and losing trades render in separate scrollable columns,
    - clicking a trade reveals its buy point, sell point, connection path, and hold duration on the chart.

## Verification (Backtest Heatmap UX Upgrade)

- `npm run lint --workspace frontend` PASS
- `npm run test --workspace frontend` PASS
- `npm run test --workspace backend` PASS
- `npm run build --workspace frontend` PASS

## UX Motion Spec

- Added robot mascot motion spec for the frontend assistant/brand bot:
  - `docs/robot-mascot-motion-spec-2026-04-04.md`
- Spec covers:
  - state priority model
  - animation table for strategy authoring, deployment, backtest, runtime pnl, risk, disconnect
  - recommended component architecture and phased implementation plan

## Continuation (Fusion Core Upgrade)

- Landed backtest decision-intensity modes with auto-threshold derivation from composite distribution:
  - `aggressive`, `neutral`, `conservative`
  - threshold is now quantile-derived from `abs(composite)` instead of fixed hard-coded constant during normal flow.
- Backtest API now accepts `decisionMode` and returns `strategy.decisionThreshold` metadata (`mode/long/short/quantile/source/sampleSize`).
- Backtest UI now includes decision-intensity selector and displays actual thresholds used in the latest run.
- Added strategy fusion explainer doc:
  - `docs/strategy-fusion-core-2026-04-04.md`

## Verification (Fusion Core Upgrade)

- `npm run lint --workspace frontend` PASS
- `npm run test --workspace backend` PASS
- `npm run build --workspace frontend` PASS
