# 2026-04-05

## Strategy Lab + Backtest Persistence Upgrade (Implemented)

- Added repository layer with optional PostgreSQL runtime:
  - `StrategyRepository` (memory + postgres)
  - `AiSettingsRepository` (memory + postgres)
  - `BacktestRepository` (memory + postgres)
- Added optional postgres client bootstrap (`DATABASE_URL` + dynamic `pg` load), with safe in-memory fallback if unavailable.

## Backend API Changes

- AI module added:
  - `GET /api/v1/ai/config`
  - `PUT /api/v1/ai/config`
  - `POST /api/v1/ai/fusion/generate`
- Backtest module upgraded:
  - `POST /api/v1/backtest/run` now returns `backtestRunId` and persists result.
  - `GET /api/v1/backtest/history`
  - `GET /api/v1/backtest/history/:id`
- `RunsService` and `BacktestService` now read strategy by `strategyId` through `StrategyRepository`.
- `StrategiesService` now persists through repository (no MemoryDb strategy authority).

## Database

- Added migration:
  - `database/migrations/0002_strategy_ai_backtest_persistence.sql`
- Updated canonical schema:
  - `database/schema.sql`
- New tables:
  - `ai_provider_settings`
  - `backtest_runs`
  - `backtest_symbol_results`
  - `backtest_portfolio_points`
  - `backtest_symbol_price_points`
  - `backtest_symbol_trade_return_points`
  - `backtest_symbol_trade_markers`

## Frontend

- API client updated:
  - AI config/fusion types and calls
  - Backtest history summary/detail types and calls
  - `BacktestResult` now includes `backtestRunId`
- `Backtest.tsx`:
  - Added Backtest History panel
  - Supports replay from persisted history detail
  - New runs auto-refresh history list
- `Strategy.tsx`:
  - Added AI Fusion panel (config + mode + generation + apply to draft)
  - Added AI result panel with total score + radar + indicator reasons
  - Strategy create now writes fusion origin metadata (`manual` / `ai_prompt` / `ai_selected`) and AI metadata
  - Persisted strategy list now shows source labels (`Manual`, `AI Prompt`, `AI Enhanced`)

## Validation

- `npm run build --workspace backend` PASS
- `npm run test --workspace backend` PASS
- `npm run build --workspace frontend` PASS
- `npm run test --workspace frontend` PASS
- `npm test` PASS
- `npm run lint --workspace frontend` FAIL (pre-existing unrelated `react-hooks/set-state-in-effect` in backtest components)

## Continuation (AI Config Persistence Hardening)

- Updated AI settings fallback persistence:
  - when `DATABASE_URL` is unavailable, AI settings now persist to local file instead of in-memory only.
  - new storage file path: `backend/.data/ai-provider-settings.json`.
- Behavior preserved:
  - frontend can modify API key/model via existing config controls.
  - leaving API key empty on update keeps the previously saved key unchanged.
- Added ignore rule to prevent local secrets file from accidental commit:
  - `.gitignore` now includes `backend/.data/`.
