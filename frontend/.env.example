# 开发：可留空，请求会走 Vite 代理到 127.0.0.1:3001（需同时启动 backend）
# VITE_API_BASE_URL=
# 若留空 WebSocket 也会走当前页面的 host + /ws 代理
# VITE_WS_BASE_URL=

# 或直接指向本机后端（需后端已监听该地址）
# VITE_API_BASE_URL=http://127.0.0.1:3001
# VITE_WS_BASE_URL=ws://127.0.0.1:3001
